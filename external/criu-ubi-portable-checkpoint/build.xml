<?xml version="1.0"?>
<project name="criu-ubi-portable-checkpoint" default="build" basedir=".">
	<taskdef resource="net/sf/antcontrib/antlib.xml" />
	<description>
		Build criu-ubi-portable-checkpoint Test
	</description>
	<import file="${TEST_ROOT}/external/build.xml"/>

	<!-- set properties for this build -->
	<property name="TEST" value="criu-ubi-portable-checkpoint" />
	<property name="DEST" value="${BUILD_ROOT}/external/${TEST}" />
	<property name="src" location="." />

	<target name="init">
		<mkdir dir="${DEST}"/>
	</target>

	<target name="build_image" depends="move_scripts,clean_image" description="build the image">
		<echo message="Executing external.sh --build --dir ${TEST} --tag ${dockerImageTag} --platform ${env.SPEC} --version ${JDK_VERSION} --impl ${JDK_IMPL} --base_docker_registry_required 'true' --docker_registry_url ${env.DOCKER_REGISTRY_URL} --docker_args ${extra_docker_args} " />
		<exec executable="bash" failonerror="true">
			<arg value="${DEST_EXTERNAL}/external.sh"/>
			<arg value="--build"/>
			<arg value="--dir"/>
			<arg value="${TEST}"/>
			<arg value="--tag"/>
			<arg value="${dockerImageTag}"/>
			<arg value="--platform"/>
			<arg value="${env.SPEC}"/>
			<arg value="--version"/>
			<arg value="${JDK_VERSION}"/>
			<arg value="--impl"/>
			<arg value="${JDK_IMPL}"/>
			<arg value="--base_docker_registry_required"/>
			<arg value="true"/>
			<arg value="--docker_registry_url"/>
			<arg value="${env.DOCKER_REGISTRY_URL}"/>
			<arg value="--docker_args"/>
			<arg value="${extra_docker_args}"/>
		</exec>
	</target>

	<target name="dist" depends="build_image" description="generate the distribution">
		<copy todir="${DEST}">
			<fileset dir="${src}" includes="*.xml, *.mk"/>
		</copy>
	</target>

	<target name="build">
		<antcall target="dist" inheritall="true" />
	</target>
</project>
